# 2026-06-04 工作记录

## 壁纸模块

### build-blog.js 改为增量模式
- Step 7.6：只生成 `wallpaper/<slug>/index.html` 不存在的壁纸（通过 `fs.existsSync` 判断）
- Step 9：clean URL 增量，已存在 `xxx/index.html` 的跳过，只创建新的
- 彻底移除 `--all` 全量生成，防止以后误触

### 3张新壁纸 SEO 完成
- `wallpaper_1780543255` — daoist-talisman-wallpaper-luck-prosperity-design
- `wallpaper_1780543209` — divine-feng-shui-wallpaper-empower-home-yuanmu
- `wallpaper_1780543087` — cosmic-energy-galaxy-wallpaper-phone-background
- 静态页已生成，sitemap 已更新，已上线

### 壁纸上线流程已固定
- 触发词：用户说"新壁纸上線了"
- 用户脚本跑图后 **不要自动 push**，等我跑完 SEO 一起 push
- 我跑完 SEO 后 **自动 commit + push**（壁纸模块例外规则）
- 流程已写入 `MEMORY.md`，不再改动

## Ahrefs 404 修复
- 问题：39个壁纸详情页 footer 链接到 `/zh/wallpaper` 和 `/zh/zodiac/zodiac-daily`，这两个URL返回404
- 根因：`dist/zh/wallpaper.html` 和 `dist/zh/zodiac/zodiac-daily.html` 不存在（没有中文版页面）
- 修复：在 `vercel.json` 加两条 301 重定向
  - `/zh/wallpaper` → `/wallpaper`
  - `/zh/zodiac/zodiac-daily` → `/zodiac/zodiac-daily`
- 已 push，Vercel 部署后 404 消失
- 最小改动方案，不重新生成壁纸、不创建新文件、零 bug 风险

## Ahrefs 规范化URL + JS 404 修复
- 问题1：14个 `/wallpaper?cat=...` 带查询参数URL缺少 canonical，Ahrefs报重复内容风险
  - 修复：`wallpaper.html` 加 `<link rel="canonical" href="https://www.daoessentia.com/wallpaper">`
- 问题2：`/blog` 页面引用不存在的 `/js/blog-wallpaper-recommendation.js`
  - 修复：移除 `build-blog.js` 里的引用
- 问题3：壁纸详情页 i18n-switcher.js 路径错误（相对路径 `js/i18n-switcher.js` 在子目录下404）
  - 修复：`vercel.json` 加 rewrite `/wallpaper/:slug/js/i18n-switcher.js` → `/js/i18n-switcher.js`
  - 不动已生成的壁纸页，零风险
- 已 build + push，Vercel 部署后全部修复

## 新壁纸上架 (wallpaper_1780233504)
- ID：`wallpaper_1780233504`
- slug：`divine-tree-wallpaper-feng-shui`
- 英文标题：Divine Tree Wallpaper - Feng Shui
- 中文标题：神树壁纸-风水布局
- 分类：Talismans / 神仙
- 已生成静态页 + 增量更新 sitemap + push ✅

## 教训
- 改 build-blog.js 后必须实际跑一次验证增量是否生效，不能只看代码
- 临时文件（如 build 日志）要及时删掉，不能留在工作区
- 壁纸详情页生成模板里的相对路径要格外小心，子目录下会404
