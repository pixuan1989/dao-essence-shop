# This file injects environment variables into the browser
# Add this to your netlify.toml or use Netlify's header injection

# For HTML files, inject STRIPE_PUBLIC_KEY as a global variable
index.html
  Content-Type: text/html

# You can use Netlify's build scripts to inject variables
# Example in netlify.toml:
# [context.production.environment]
#   STRIPE_PUBLIC_KEY = "pk_live_..."
# [context.deploy-preview.environment]
#   STRIPE_PUBLIC_KEY = "pk_test_..."
